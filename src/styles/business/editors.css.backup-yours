.biz-svc {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
}

.biz-svc__list {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    margin: 0;
    padding: 0;
    list-style: none;
}

.biz-svc__item {
    border-radius: var(--lc-r-lg);
    background: var(--lc-raised);
    box-shadow: var(--lc-shadow-raised);
    animation: biz-svc-in var(--lc-base) var(--lc-ease);
}

.biz-svc__item.is-open {
    background: var(--lc-card);
    box-shadow: inset 0 0 0 1px var(--lc-line-strong);
}

.biz-svc__item.has-error {
    box-shadow: inset 0 0 0 1px rgba(240, 122, 110, 0.45);
}

@keyframes biz-svc-in {
    from { opacity: 0; transform: translateY(6px); }
}

.biz-svc__summary {
    display: flex;
    align-items: center;
    gap: var(--space-1);
    padding-right: var(--space-2);
}

.biz-svc__open {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto auto;
    grid-template-areas: 'name price chevron' 'meta price chevron';
    align-items: center;
    column-gap: var(--space-3);
    flex: 1;
    min-width: 0;
    min-height: 64px;
    padding: var(--space-3) var(--space-4);
    border: 0;
    border-radius: var(--lc-r-lg);
    background: none;
    color: var(--lc-ink-1);
    font-family: var(--lc-font-body);
    text-align: left;
    cursor: pointer;
    transition: transform var(--lc-fast) var(--lc-ease);
}

.biz-svc__open:active { transform: scale(0.99); }

.biz-svc__open:focus-visible {
    outline: 2px solid var(--azure);
    outline-offset: 2px;
}

.biz-svc__name {
    grid-area: name;
    overflow: hidden;
    font-size: var(--lc-t-body);
    font-weight: var(--font-weight-semibold);
    text-overflow: ellipsis;
    white-space: nowrap;
}

.biz-svc__meta {
    grid-area: meta;
    color: var(--lc-ink-3);
    font-size: var(--lc-t-label);
}

.biz-svc__price {
    grid-area: price;
    color: var(--lc-ink-1);
    font-family: var(--lc-font-mono);
    font-size: var(--lc-t-body);
    font-variant-numeric: tabular-nums;
}

.biz-svc__chevron {
    grid-area: chevron;
    color: var(--lc-ink-4);
}

.biz-svc__order {
    display: flex;
    gap: 2px;
    transition: opacity var(--lc-fast) var(--lc-ease);
}

@media (hover: hover) and (pointer: fine) {
    .biz-svc__order { opacity: 0; }

    .biz-svc__item:hover .biz-svc__order,
    .biz-svc__item:focus-within .biz-svc__order { opacity: 1; }
}

.biz-svc__order .ui-iconbtn:disabled {
    opacity: 0.3;
    cursor: default;
    pointer-events: none;
}

.biz-svc__form {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
    padding: var(--space-5);
}

.biz-svc__row {
    display: flex;
    flex-wrap: wrap;
    align-items: flex-end;
    gap: var(--space-3);
}

.biz-svc__row > .ui-field:first-child { flex: 1 1 280px; }
.biz-svc__row > .ui-field:nth-child(2) { flex: 0 1 120px; }

.biz-svc__formactions {
    display: flex;
    justify-content: space-between;
    gap: var(--space-2);
}

.biz-svc__add {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-4);
}

.biz-svc__suggest {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
}

.biz-svc__suggestlabel {
    color: var(--lc-ink-3);
    font-size: var(--lc-t-label);
}

.biz-svc__chipmeta {
    color: var(--lc-ink-3);
    font-family: var(--lc-font-mono);
    font-size: var(--lc-t-micro);
}


.biz-sched {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-4);
}

.biz-sched__templates {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: var(--space-2);
    width: 100%;
}

.biz-sched__template {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 6px;
    padding: var(--space-3) var(--space-4);
    border: 0;
    border-radius: var(--lc-r-lg);
    background: var(--lc-card);
    box-shadow: inset 0 0 0 1px var(--lc-line);
    color: var(--lc-ink-1);
    font-family: var(--lc-font-body);
    text-align: left;
    cursor: pointer;
    transition: box-shadow var(--lc-fast) var(--lc-ease), background var(--lc-fast) var(--lc-ease), transform var(--lc-fast) var(--lc-ease);
}

.biz-sched__template:active { transform: scale(0.98); }

.biz-sched__template.is-on {
    background: var(--lc-raised);
    box-shadow: inset 0 0 0 1.5px var(--azure), var(--lc-shadow-raised);
}

.biz-sched__template:focus-visible {
    outline: 2px solid var(--azure);
    outline-offset: 2px;
}

@media (hover: hover) {
    .biz-sched__template:not(.is-on):hover { background: var(--lc-raised); }
}

.biz-sched__ttitle {
    font-size: var(--lc-t-label);
    font-weight: var(--font-weight-semibold);
}

.biz-sched__tdetail {
    color: var(--lc-ink-3);
    font-size: 12px;
}

.biz-glyph {
    display: inline-flex;
    gap: 3px;
}

.biz-glyph span {
    position: relative;
    width: 12px;
    height: 16px;
    border-radius: 3px;
    box-shadow: inset 0 0 0 1px var(--lc-line-strong);
}

.biz-glyph span.is-open {
    background: var(--azure);
    box-shadow: none;
}

.biz-glyph span.has-lunch::after {
    content: '';
    position: absolute;
    top: 7px;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--lc-canvas);
}

.biz-sched__cards {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    width: 100%;
    margin: 0;
    padding: 0;
    list-style: none;
}

.biz-sched__card {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    padding: var(--space-4);
    border-radius: var(--lc-r-lg);
    background: var(--lc-card);
    box-shadow: inset 0 0 0 1px var(--lc-line);
    animation: biz-svc-in var(--lc-base) var(--lc-ease);
}

.biz-sched__card.has-error { box-shadow: inset 0 0 0 1px rgba(240, 122, 110, 0.45); }

.biz-sched__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2);
}

.biz-sched__days {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
}

.biz-sched__day {
    display: grid;
    place-items: center;
    width: 36px;
    height: 36px;
    padding: 0;
    border: 0;
    border-radius: var(--radius-full);
    background: none;
    box-shadow: inset 0 0 0 1px var(--lc-line-strong);
    color: var(--lc-ink-2);
    font-family: var(--lc-font-body);
    font-size: 13px;
    font-weight: var(--font-weight-semibold);
    cursor: pointer;
    transition: background var(--lc-fast) var(--lc-ease), color var(--lc-fast) var(--lc-ease), transform var(--lc-fast) var(--lc-ease);
}

.biz-sched__day:active { transform: scale(0.94); }

.biz-sched__day.is-on {
    background: var(--lc-azure-button);
    box-shadow: none;
    color: var(--lc-on-accent);
}

.biz-sched__day[aria-disabled='true'] {
    box-shadow: inset 0 0 0 1px var(--lc-line);
    color: var(--lc-ink-4);
    cursor: default;
}

.biz-sched__day:focus-visible {
    outline: 2px solid var(--azure);
    outline-offset: 2px;
}

.biz-sched__line {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-2);
}

.biz-sched__label {
    width: 52px;
    color: var(--lc-ink-3);
    font-size: var(--lc-t-label);
}

.biz-sched__to {
    color: var(--lc-ink-3);
    font-size: var(--lc-t-label);
}

.biz-sched__none {
    color: var(--lc-ink-3);
    font-size: var(--lc-t-label);
}

.biz-sched__switch { margin-left: auto; }

.biz-sched__error {
    margin: 0;
    color: var(--lc-danger);
    font-size: 12px;
    font-weight: var(--font-weight-medium);
}

.biz-sched__summary {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    width: 100%;
    padding-top: var(--space-3);
    border-top: 1px solid var(--lc-line);
}

.biz-sched__summary .biz-glyph { flex: none; margin-top: 3px; }

.biz-sched__summary p {
    margin: 0;
    color: var(--lc-ink-2);
    font-size: var(--lc-t-label);
    line-height: var(--lc-lh-snug);
}

@media (prefers-reduced-motion: reduce) {
    .biz-svc__item,
    .biz-sched__card {
        animation: none;
    }

    .biz-sched__day,
    .biz-sched__template { transition: none; }

    .biz-svc__order { transition: none; }

    .biz-svc__open { transition: none; }
}

@media (max-width: 767px), (pointer: coarse) {
    .biz-svc .ui-btn--sm,
    .biz-sched .ui-btn--sm,
    .biz-svc .ui-chip {
        min-height: 44px;
    }

    .biz-svc .ui-iconbtn--sm,
    .biz-sched .ui-iconbtn--sm {
        width: 44px;
        height: 44px;
    }

    .biz-sched__day { width: 44px; height: 44px; }

    .biz-sched .ui-picker-trigger--sm {
        min-height: 44px;
    }
}

